# Database
DB_HOST="localhost"
DB_PORT=5432
DB_NAME=""               # Keep SQL identifiers lowercase.
DB_USERNAME=""           # Keep SQL identifiers lowercase.
DB_PASSWORD=""

# HTTP
PORT=3000   # PORT is commonly injected on deployment platforms like Render.
